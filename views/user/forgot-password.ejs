<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nhập mã xác minh - Web Hướng Nghiệp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Element UI CSS -->
    <link rel="stylesheet" href="https://unpkg.com/element-ui/lib/theme-chalk/index.css">
    <style>
        body {
            background: linear-gradient(135deg, #4CAF50, #2E7D32);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: Arial, sans-serif;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }
        .verification-container {
            background: #ffffff;
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 900px;
            margin: auto;
        }
        .logo-container {
            margin-bottom: 1.5rem;
        }
        .logo {
            width: 150px;
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
            color: inherit;
        }
        .content-container {
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }
        .verification-form {
            flex: 1;
            width: 100%;
        }
        .verification-image {
            flex: 1;
            display: flex;
            align-items: flex-start;
            justify-content: center;
        }
        .verification-image img {
            max-width: 100%;
            height: auto;
            background: #f5f5f5;
            border-radius: 16px;
            width: 400px;
        }
        .form-control {
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 15px;
        }
        .form-control:focus {
            box-shadow: none;
            border-color: #4CAF50;
        }
        .btn-verify {
            background-color: #4CAF50;
            border: none;
            border-radius: 8px;
            padding: 12px;
            width: 100%;
            color: white;
            font-weight: 600;
            text-transform: none;
            font-size: 14px;
            margin-top: 1rem;
        }
        .btn-verify:hover {
            background-color: #45a049;
        }
        .back-link {
            color: #666;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            margin-bottom: 1.5rem;
            font-size: 14px;
        }
        .back-link:hover {
            color: #4CAF50;
        }
        .email-display {
            color: #666;
            margin-bottom: 1.5rem;
            font-size: 14px;
            line-height: 1.5;
        }
        .email-display strong {
            color: #FF8C00;
            font-weight: 600;
        }
        .resend-text {
            color: #666;
            font-size: 13px;
            margin-right: 5px;
        }
        .resend-link {
            color: #FF0000;
            text-decoration: none;
            font-size: 13px;
        }
        .resend-link:hover {
            text-decoration: underline;
        }
        .resend-link.disabled {
            color: #999;
            pointer-events: auto;
            text-decoration: none;
            cursor: not-allowed;
            opacity: 0.6;
        }
        .countdown {
            color: #666;
            font-size: 13px;
            margin-left: 5px;
        }
        .password-field {
            position: relative;
        }
        .password-toggle {
            position: absolute;
            right: 12px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #666;
            padding: 4px;
        }
        .el-form-item__error {
            color: #F56C6C;
            font-size: 12px;
            line-height: 1;
            padding-top: 4px;
            position: absolute;
            top: 100%;
            left: 0;
        }
        .verification-code {
            letter-spacing: 4px;
            font-size: 14px;
        }
        /* Tùy chỉnh style của Element UI để phù hợp với thiết kế */
        .el-input__inner {
            height: 44px !important;
            font-size: 14px !important;
            border-radius: 8px !important;
        }
        .el-input__inner:focus {
            border-color: #4CAF50 !important;
            box-shadow: none !important;
        }
        .el-form-item {
            margin-bottom: 20px !important;
        }

        /* Responsive styles */
        @media (min-width: 993px) {
            .verification-container {
                padding: 2.5rem;
                border: none;
                box-shadow: none;
            }
            .content-container {
                flex-direction: row;
                align-items: flex-start;
                gap: 4rem;
            }
            .verification-form {
                padding-top: 2rem;
                max-width: 400px;
                width: 400px;
            }
            .logo-container {
                margin-bottom: 0;
            }
        }

        @media (max-width: 992px) {
            .verification-container {
                max-width: 600px;
            }
            .verification-form {
                padding: 20px 0;
            }
            .verification-image {
                padding: 0 0 20px 0;
            }
            .verification-image img {
                max-width: 80%;
            }
        }

        @media (max-width: 576px) {
            .container {
                padding: 10px;
            }
            .verification-container {
                padding: 1.5rem;
            }
            h2 {
                font-size: 1.5rem;
            }
            .email-display {
                font-size: 12px;
            }
            .email-display strong {
                font-size: 13px;
            }
            .logo {
                width: 120px;
            }
            .logo img {
                height: 28px;
            }
            .el-input__inner {
                height: 38px !important;
                font-size: 13px !important;
            }
            .btn-verify {
                height: 42px;
                font-size: 14px;
            }
            .resend-text, .resend-link, .countdown {
                font-size: 12px;
            }
            .verification-code {
                letter-spacing: 6px;
                font-size: 14px;
            }
        }

        @media (max-width: 360px) {
            .verification-container {
                padding: 1rem;
            }
            h2 {
                font-size: 1.3rem;
            }
            .email-display {
                font-size: 11px;
            }
            .email-display strong {
                font-size: 12px;
            }
            .verification-code {
                letter-spacing: 4px;
                font-size: 13px;
            }
        }

        h2 {
            font-size: 24px;
            margin-bottom: 1rem;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="container" id="app">
        <div class="verification-container">
            <div class="logo-container">
                <a href="/" class="logo">
                    <img src="/images/logo.svg" alt="CareerTest">
                </a>
            </div>
            <div class="content-container">
                <div class="verification-form">
                    <a href="/login" class="back-link">
                        <i class="fas fa-arrow-left me-2"></i>
                        Quay trở lại màn đăng nhập
                    </a>
                    <h2 class="mb-4">Nhập mã xác minh</h2>
                    <p class="email-display">
                        Vui lòng nhập mã xác minh đã được gửi về địa chỉ email
                        <br>
                        <strong>lynguyen55@gmail.com</strong> của bạn
                    </p>
                    <el-form :model="formData" :rules="rules" ref="verificationForm">
                        <el-form-item prop="verificationCode">
                            <div class="password-field">
                                <el-input 
                                    v-model="formData.verificationCode"
                                    maxlength="6"
                                    class="verification-code"
                                    placeholder="Nhập mã xác minh"
                                    @input="handleInput">
                                </el-input>
                                <span class="password-toggle">
                                    <i class="far fa-eye"></i>
                                </span>
                            </div>
                        </el-form-item>
                        <p>
                            <span class="resend-text">Chưa nhận được mã xác minh?</span>
                            <a href="#" 
                               :class="['resend-link', {disabled: isCountingDown}]" 
                               @click.prevent="handleResend">Gửi lại</a>
                            <span class="countdown" v-if="countdown > 0">({{formatCountdown}})</span>
                        </p>
                        <button type="submit" class="btn btn-verify" @click.prevent="submitForm">Xác nhận</button>
                    </el-form>
                </div>
                <div class="verification-image">
                    <img src="/images/verification.svg" alt="Verification Illustration">
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/vue@2/dist/vue.js"></script>
    <script src="https://unpkg.com/element-ui/lib/index.js"></script>
    <script src="https://unpkg.com/element-ui/lib/umd/locale/vi.js"></script>
    <script>
        ELEMENT.locale(ELEMENT.lang.vi)
        
        new Vue({
            el: '#app',
            data() {
                return {
                    formData: {
                        verificationCode: ''
                    },
                    rules: {
                        verificationCode: [
                            { required: true, message: 'Vui lòng nhập mã xác minh', trigger: 'blur' },
                            { min: 6, max: 6, message: 'Mã xác minh phải có 6 ký tự', trigger: 'blur' }
                        ]
                    },
                    countdown: 60,
                    timer: null
                }
            },
            computed: {
                isCountingDown() {
                    return this.countdown > 0;
                },
                formatCountdown() {
                    const minutes = Math.floor(this.countdown / 60);
                    const seconds = this.countdown % 60;
                    return `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
                }
            },
            methods: {
                handleInput(value) {
                    // Chỉ cho phép nhập số
                    this.formData.verificationCode = value.replace(/[^\d]/g, '');
                },
                startCountdown() {
                    this.countdown = 60;
                    clearInterval(this.timer);
                    this.timer = setInterval(() => {
                        if (this.countdown > 0) {
                            this.countdown--;
                        } else {
                            clearInterval(this.timer);
                        }
                    }, 1000);
                },
                handleResend() {
                    if (!this.isCountingDown) {
                        // TODO: Gọi API gửi lại mã
                        this.$message({
                            message: 'Đã gửi lại mã xác minh',
                            type: 'success'
                        });
                        this.startCountdown();
                    }
                },
                submitForm() {
                    this.$refs.verificationForm.validate((valid) => {
                        if (valid) {
                            // TODO: Xử lý submit form
                            console.log('Form is valid', this.formData);
                        } else {
                            return false;
                        }
                    });
                }
            },
            mounted() {
                this.startCountdown();
            },
            beforeDestroy() {
                clearInterval(this.timer);
            }
        });
    </script>
</body>
</html> 