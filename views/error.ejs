
<div class="container-fluid">
    <div class="text-center">
        <div class="error mx-auto" data-text="Error">Error</div>
        <p class="lead text-gray-800 mb-5">Đã có lỗi xảy ra</p>
        <p class="text-gray-500 mb-0"><%= message %></p>
        <% if (error.stack) { %>
            <pre class="mt-4 text-left text-gray-600"><%= error.stack %></pre>
        <% } %>
        <a href="/">&larr; Quay về trang chủ</a>
    </div>
</div>
